let api_host = '';

export default {
  home_list: `${api_host}/cdn/zndzgzt/selectAll`, //  // `${api_host}fizz/v1/list`,//
  find_list: `${api_host}fizz/v1/find`,  // selectOne
  user_move: `${api_host}/cdn/zndzgzt/changeRy`,
}

